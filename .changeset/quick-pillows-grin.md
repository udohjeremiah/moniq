---
"@udohjeremiah/moniq": minor
---

Add file policies to the `files` config domain to require, forbid, and validate files in each package, with `moniq check` validation and `moniq fix` create/delete actions.
