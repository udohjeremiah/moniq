---
"@udohjeremiah/moniq": patch
---

Refactor pretty formatter to ESLint-inspired output with brand purple colors, fix `init` command workspace root validation, and clean up internal return types and barrel exports
