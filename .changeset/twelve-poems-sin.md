---
"@moniq/cli": patch
"@moniq/workspace": patch
---

Add pnpm `-w` flag for workspace root installs, capture stderr on install failure, and throw on unknown package manager instead of defaulting to pnpm
